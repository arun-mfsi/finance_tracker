import User from './User.js';
import Transaction from './Transaction.js';

// Model associations and relationships will be defined here

// Export all models
export { User, Transaction };

// Default export for convenience
export default {
  User,
  Transaction,
};
